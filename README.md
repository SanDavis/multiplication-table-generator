# Notas:
Este es mi primer programa con node

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Base de la tabla de multiplicar                     [requerido]
  -h, --hasta    Indica hasta qué numero se ha de generar la tabla[requerido]
```